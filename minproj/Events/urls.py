from django.urls import path

# from .views import Create, Detail, Update, UpdateByCoordinator, Delete, EventsAll


urlpatterns = [
    # path('', EventsAll.as_view(), name='events_all'),
    # path('create/', Create.as_view(), name='event_create'),
    # path('<int:pk>/update/', Update.as_view(), name='location_update'),
    # path('<int:pk>/edit/', UpdateByCoordinator.as_view(), name='location_edit'),
    # path('<int:pk>/delete/', Delete.as_view(), name='location_delete'),
    # path('<int:pk>/', Detail.as_view(), name='event'),
   ]
